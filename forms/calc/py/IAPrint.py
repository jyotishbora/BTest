from vb2py.vbfunctions import *
from vb2py.vbdebug import *



def AAltTax_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.AAltTax)
    else:
        ReturnVal = 0

def ABal1_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ABal1)
    else:
        ReturnVal = 0

def ABal2_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ABal2)
    else:
        ReturnVal = 0

def ABal4_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ABal4)
    else:
        ReturnVal = 0

def ABal36_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ABal36)
    else:
        ReturnVal = 0

def ABal3_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ABal3)
    else:
        ReturnVal = 0

def ABalance_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ABalance)
    else:
        ReturnVal = 0

def ACredits_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ACredits)
    else:
        ReturnVal = 0

def ACrNon_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ACrNon)
    else:
        ReturnVal = 0

def ADedBox_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ADedBox)
    else:
        ReturnVal = 0

def AEstTax_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.AEstTax)
    else:
        ReturnVal = 0

def AExemCr_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.AExemCr)
    else:
        ReturnVal = 0

def AFedDed_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.AFedDed)
    else:
        ReturnVal = 0

def AFedTax_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.AFedTax)
    else:
        ReturnVal = 0

def AIAMin_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.AIAMin)
    else:
        ReturnVal = 0

def ALump_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ALump)
    else:
        ReturnVal = 0

def AOthIACr_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.AOthIACr)
    else:
        ReturnVal = 0

def AOutState_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.AOutState)
    else:
        ReturnVal = 0

def APrior_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.APrior)
    else:
        ReturnVal = 0

def ARefund_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ARefund)
    else:
        ReturnVal = 0

def ASch_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ASch)
    else:
        ReturnVal = 0

def ASelf_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ASelf)
    else:
        ReturnVal = 0

def ATaxB4Cont_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ATaxB4Cont)
    else:
        ReturnVal = 0

def ATaxInc_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ATaxInc)
    else:
        ReturnVal = 0

def ATaxWith_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ATaxWith)
    else:
        ReturnVal = 0

def ATotIATax_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ATotIATax)
    else:
        ReturnVal = 0

def ATotTax_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ATotTax)
    else:
        ReturnVal = 0

def ATuit_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.ATuit)
    else:
        ReturnVal = 0

def AVolFireCr_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.AVolFireCr)
    else:
        ReturnVal = 0

def BAltTax_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BAltTax)
    else:
        ReturnVal = 0

def BBal1_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BBal1)
    else:
        ReturnVal = 0

def BBal2_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BBal2)
    else:
        ReturnVal = 0

def BBal4_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BBal4)
    else:
        ReturnVal = 0

def BBal36_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BBal36)
    else:
        ReturnVal = 0

def BBal3_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BBal3)
    else:
        ReturnVal = 0

def BBalance_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BBalance)
    else:
        ReturnVal = 0

def BCredits_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BCredits)
    else:
        ReturnVal = 0

def BCrNon_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BCrNon)
    else:
        ReturnVal = 0

def BDedBox_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BDedBox)
    else:
        ReturnVal = 0

def BEstTax_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BEstTax)
    else:
        ReturnVal = 0

def BExemCr_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BExemCr)
    else:
        ReturnVal = 0

def BFedDed_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BFedDed)
    else:
        ReturnVal = 0

def BFedTax_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BFedTax)
    else:
        ReturnVal = 0

def BIAMin_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BIAMin)
    else:
        ReturnVal = 0

def BLump_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BLump)
    else:
        ReturnVal = 0

def BOthIACr_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BOthIACr)
    else:
        ReturnVal = 0

def BOutState_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BOutState)
    else:
        ReturnVal = 0

def BPrior_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BPrior)
    else:
        ReturnVal = 0

def BRefund_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BRefund)
    else:
        ReturnVal = 0

def BSch_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BSch)
    else:
        ReturnVal = 0

def BSelf_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BSelf)
    else:
        ReturnVal = 0

def BTaxB4Cont_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BTaxB4Cont)
    else:
        ReturnVal = 0

def BTaxInc_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BTaxInc)
    else:
        ReturnVal = 0

def BTaxWith_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BTaxWith)
    else:
        ReturnVal = 0

def BTotIATax_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BTotIATax)
    else:
        ReturnVal = 0

def BTotTax_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BTotTax)
    else:
        ReturnVal = 0

def BTuit_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BTuit)
    else:
        ReturnVal = 0

def BVolFireCr_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.BVolFireCr)
    else:
        ReturnVal = 0

def FirstName_Calculation():
    ReturnVal = GetString(IAF1040.FirstName)

def ItemCheck_Calculation():
    if GetBool(IARequired.LowInc) == True:
        ReturnVal = 0
    else:
        ReturnVal = GetBool(IAF1040.ItemCheck)

def LastName_Calculation():
    ReturnVal = GetString(IAF1040.LastName)

def LowInc_Calculation():
    if GetBool(IARequired.LowInc) == True:
        ReturnVal = 0
    else:
        ReturnVal = 1

def SSN_Calculation():
    ReturnVal = GetString(IAF1040.SSN)

def StadCheck_Calculation():
    if GetBool(IARequired.LowInc) == True:
        ReturnVal = 0
    else:
        ReturnVal = GetBool(IAF1040.StadCheck)

def TotalTax_Calculation():
    if GetNumber(IAPrint.LowInc) == 1:
        ReturnVal = GetCurrency(IAF1040.TotalTax)
    else:
        ReturnVal = 0

